package rules.generator;

import rules.common.Rule;
import rules.common.RuleType;

public class RuleGenerator {
    public static Rule generate(double difficulty, double tolerance, RuleType... allowedrules){
        // TODO: Given a difficulty and a set of RuleTypes I can use, generate a randomrule that equals the requested difficulty +- tolerance%
        return null;
    }
}
